package com.miniworldmap;

import net.runelite.client.config.Config;
import net.runelite.client.config.ConfigGroup;

@ConfigGroup("miniworldmap")
public interface MiniWorldMapConfig extends Config
{
	// later you could add options like map size, position, etc.
}